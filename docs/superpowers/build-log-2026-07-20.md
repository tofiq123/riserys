# Rise — overnight autonomous build log (2026-07-20 → 21)

**Context:** built autonomously while you slept, per your instruction to complete as much of the master roadmap as possible for both platforms. Everything here is **code-complete + `flutter analyze`-clean**, but **not device-tested and not compiled on iOS** (no Mac here) — that's your morning pass. This log is the record + the punch-list.

**How I gated it:** `flutter analyze` clean = my "it compiles" proxy (no emulator run). Pure-Dart logic has unit tests kept green (they run headless). Native mostly comes from maintained pub.dev plugins; permissions wired in `AndroidManifest.xml` + iOS `Info.plist`/entitlements. Each phase built on its own branch, merged to `main` when analyze-clean. Migrations are written but **not applied** (you run them); edge functions I deployed myself.

---

## ✅ Completed (on `main` unless noted)

### Phase 6 — Alertness Engine (mini-PVT)  · branch `phase6-alertness`
- 6.1 PVT metrics engine (`lib/domain/pvt.dart`) — commit `7202e81`, 5/5 tests, analyze clean.
- 6.2 Alertness score 0–100 — commit `d551023`, 9/9 tests, analyze clean.
- 6.3 PVT mission widget (`lib/ui/missions/pvt_mission.dart`) — commit `df9bba1`, anti-trap invariant verified.
- 6.4 wire PVT into picker + ring — commit `8f2f9bc`.
- 6.5 persist alertness score (Drift **schema v4→v5**, idempotent add-column) — commit `b989f39`.
- 6.6 Stats "Alertness" card (honest copy, neutral bands, no red) — commit `5eacfb4`.
- **Phase 6 MERGED to main** (`1ee1cbb`). Full suite 380/380, analyze clean.

### Phase 7 — Compassion & Intentions  · **MERGED to main** (`285777e`)
- 7.1 anti-shame tone pass (`d0cd19f`) — copy was already mostly kind; softened onboarding "Prove you're awake" → "Wake up all the way" + forward-looking empty states.
- 7.2 rough-night streak exemption (`6e93b7c`) — new `excused_days` table (**schema v5→v6**, idempotent), `computeStreak` `excusedDays` param (excused miss → neutral, abuse-safe), Stats "Rough night?" card (Today/Yesterday). Also hardened `streakProvider` against a synchronous StateError (production fix).
- 7.3 implementation-intentions (`b55957a`) — `RiseSettings.wakeIntention`, skippable onboarding step w/ suggestion chips, shown on ring screen ("Your plan: …"), editable in Settings.
- 7.4 warmer permission priming (`a0ddcd6`).
- Full suite 401/401, analyze clean.

### Phase 8 — Rhythm & Care  · **MERGED to main**
- 8.1 target wake-time anchor (`9f4ac53`, settings + onboarding "Sleep goal") · 8.2 gradual-shift `nextShiftedWakeTime` (`ab78203`, opt-in Apply on Home, never silent) · 8.3 PHQ-2 check-in (`bb59973`, Profile-only, stateless, warm+non-diagnostic, findahelpline.com off-ramp, disclaimer) · 8.4 honest insights (`67d8391`, gated ≥5 wake-ups). No Drift migration. 440/440, analyze clean.
- **Judgment:** promoted `url_launcher` to a direct dependency (guarded launch); onboarding is now 5 pages.
- **Device punch-list:** verify the PHQ-2 helpline link opens (url_launcher; may need iOS Info.plist / Android `<queries>` for https — verify); verify shift "Apply" reschedules correctly; walk the 5-page onboarding.
- **Copy for your second pass:** PHQ-2 result messages + insight one-liners (both written to be warm/non-judgemental; a test bars abnormal/irregular/disorder/lazy/bad/poor/fail).

### Phase 9 — Missions Overhaul  · **MERGED to main** (`db3fa24`)
- 9A (pure Dart): mission chains (`Alarm.missionCount`, **schema v6→v7**, ring-gate sequencing verified), opt-in adaptive difficulty (`adaptiveDifficulty` bumps a tier after 3 clean mission wake-ups), typing mission. `31c9029`/`7cc509d`/`a4de311`.
- 9B (device): shake / QR / walk-N-steps missions + `Alarm.missionData` (**schema v7→v8**). Deps **sensors_plus 6.1.2, mobile_scanner 5.2.3, pedometer 4.2.0** (`pub get` resolved; stayed on stable lines, not the 7.x majors). `9523532`/`71a9ccd`/`ff27210`/`edb7a80`. **495 tests, analyze clean.**
- **Deferred (device session):** photo-match + PERCLOS/camera-face missions (heaviest native: camera + ML Kit; need on-device tuning).
- **Native added:** Android `CAMERA` + `ACTIVITY_RECOGNITION`; iOS `NSCameraUsageDescription` + `NSMotionUsageDescription`.
- **⚠️ Device punch-list (Phase 9):** (1) first native compile with the 3 plugins — watch Android minSdk/compileSdk (mobile_scanner MLKit) + iOS deployment target (mobile_scanner wants iOS 12+); (2) **QR `controller.start()` timing** — highest-risk native detail, verify preview turns on; (3) tune shake 2.7 g threshold on a real device; (4) steps permission + fallback-to-slide path; (5) migration v7→v8 upgrade on an existing install preserves data.

### Phase 13 — Stats, Insights & Achievements  · **MERGED to main**
- 13.1 achievements engine + badges wall (`ce30d94`, 8 action-framed badges read `StreakStats.best` so never un-earn) · 13.2 consistency score + alertness sparkline trend (`3d3130a`) · 13.3 shareable stats card via `RepaintBoundary`→PNG→**share_plus ^12.0.2** (`d3a469f`) · 13.4 Week/Month/Year views (`2adf106`). No migration. **548/548, analyze clean.**
- **Device punch-list:** the share capture (`toImage`) + OS share sheet need one on-device pass (can't run headless).

### Phase 12 — Reliability & Setup Guardian  · **MERGED to main**
- Setup Guardian dashboard (`0ed3181`), OEM-aware battery guidance via `device_info_plus 11.5.0` (`2221699`), first-alarm wizard (`340c076`). Onboarding now 6 pages. **577 tests, analyze clean.**
- **✅ Audit result (good news):** the Android engine ALREADY uses `AlarmManager.setAlarmClock()` (Doze-exempt) with all correct manifest perms (`USE_EXACT_ALARM`, `SCHEDULE_EXACT_ALARM`, `USE_FULL_SCREEN_INTENT`, battery-opt, boot). Native left untouched (empty Android diff). Core reliability is sound.
- **Device punch-list:** verify each "Fix" opens the right system screen; `device_info_plus` maps real `Build.MANUFACTURER` to the right vendor steps; OEM menu-path copy accuracy per skin/version; `setAlarmClock` fires through Doze after overnight idle on Samsung/Xiaomi.

### Phase 11 — Wake-Confidence & Stay-Up  · **MERGED to main**
- Sustained-motion consensus (`5edf123`, reuses pedometer, threshold 12 steps) + `wakeConfidence` fusion (motion 60 / interaction 25 / alertness 0–15, confident ≥60 so only real motion clears the bar) + `smartWakeCheck` toggle **OFF by default** (`bc37fa0`). No native/plugin change. **614 tests, analyze clean.**
- **⚠️ Important tradeoff (opt-in only):** suppressing a re-ring without a native cancel means the smart path *defers* arming the native re-ring until after the sensing window — so if the app is killed mid-window, nothing is armed. Only affects users who turn the setting ON. **Before making this default, add a native "cancel wake-check" (arm-then-cancel) so the safety net is always present.** Default path (setting OFF) is byte-for-byte unchanged.
- **Device punch-list:** on-device motion sensing collects real samples + fires `sustained` after a real walk; end-to-end smart path (get up → no re-ring; stay in bed → re-ring); OFF regression unchanged; no "you fell back asleep" framing.

### Phase 14 — Crew Depth & Consequence (client-side)  · **MERGED to main**
- Friend detail screen (`6eb3a42`), hybrid crew score (`f334cf9`), accountability ping (`7b0b318`), leaderboard→detail nav (`51ebfb3`). No `supabase/` or plugin changes. **647 tests, analyze clean.** Build-verified — needs wired backend + 2 accounts.
- **For your review:** crew-score formula = `currentStreak*3 + round(onTimeRate*10)` per member (display-only; tune to taste). Accountability ping delivers the *standard* nudge push (custom "I'm back on it" copy needs the deferred backend); in-app framing carries the message.
- **Deferred (backend session):** activity feed, reactions, server streak-break fanout, custom ping copy.

### Phase 17 — Monetization  · **MERGED to main**
- RevenueCat entitlement service (`3b3f8f2`), hard paywall (`a258e60`, $4.99/mo·$39.99/yr·$79.99 lifetime, no trial), premium registry + gates (`2c53c78`). **purchases_flutter 10.4.2. 678 tests, analyze clean.**
- **Keyless build = fully unlocked** (no behavior change on merge). Free keeps: reliable alarm, Setup Guardian, basic missions + the PVT hook, basic stats, PHQ-2, ≤3 crew. Premium: advanced/extra missions, chains, adaptive, smart wake-check, trends, monthly/yearly, shareable card, unlimited crew. **Reliability + PHQ-2 never gated; dismissing a saved premium mission never blocked.**
- **Account punch-list:** set `REVENUECAT_API_KEY` (+ optional `REVENUECAT_ENTITLEMENT_ID`, default `premium`) via `--dart-define`; RevenueCat dashboard entitlement `premium` + a *current* offering (monthly/annual/lifetime); App Store Connect / Play products at target prices linked to RevenueCat; sandbox purchase + Restore on device; iOS In-App-Purchase capability on the Mac pass.

### Phase 10 — Sensory wake (slice)  · **MERGED to main**
- Sunrise wake visual (`614689b`, animated dawn gradient behind the ring + graceful `screen_brightness 2.1.11` ramp, restored on dismiss) + "get real light" prompt (`6f82fcc`, **defaulted OFF** by fix `73c7f0b` to keep the critical ring layout unchanged). **695 tests, analyze clean. No native/audio/migration touched.**
- **Deferred:** native volume fade-in (don't touch working ring audio), real CC0 sound *assets* (you supply the audio files), per-alarm vibration patterns (native).
- **Device punch-list:** sunrise brightness ramps + **restores** on dismiss/snooze/kill; dawn gradient keeps the clock legible; reduce-motion → static; verify the (opt-in) light prompt doesn't clip the ring on your device.

### ✅ CHECKPOINT — integrated `main` verified: **695 tests pass, analyze clean** (ran on main after all merges).

### Phase 18 — Polish (icon + l10n)  · **MERGED to main**
- Brand launcher icon (`63c5a4e`, generated sunrise mark via `tool/gen_icon.dart` + `flutter_launcher_icons`; Android adaptive + iOS AppIcon set) + l10n scaffolding (`e35d2e1`, `flutter_localizations`/`intl` + `l10n.yaml` + `app_en.arb` + delegates wired; NO string migration → zero churn; see `docs/l10n.md`). **695 tests, analyze clean.**
- **Device punch-list:** glance at the composited Android adaptive icon + masked iOS corners on-device (geometry is safe-zone-correct but couldn't render headless).
- Dark mode, HealthKit, home widget still deferred (visual/native).

### Phase 15 — Groups & Challenges (backend, build-verified)  · **MERGED to main**
- Groups schema + RLS + `create_group`/`join_group_by_code` RPCs (`c050380`), service/providers/Groups sub-tab + group leaderboard (`188e624`), security hardening (`e23054d`). Migration **`supabase/migrations/0006_groups.sql`**. **728 tests, analyze clean. Deployed nothing.**
- **RLS reviewed + hardened** (I adversarially reviewed it): SECURITY DEFINER membership helpers (no policy recursion), `set search_path=''`, guard triggers freezing ownership/role, members-only reads. I removed the direct member self-insert policy so the **invite code is the sole way to join** (a leaked group uuid grants nothing).
- **⚠️ You must:** apply `0006_groups.sql` in Supabase; then 2-account test (create group, share code, join, group leaderboard shows both, non-member can't read, owner-only manage, owner can't leave). Challenges + deep-link invites deferred.

### Phase 16 — Voice wake-ups (backend + device audio)  · branch `phase16-voice` · *in progress*
voice_clips + Storage bucket + RLS + record (≤30s) + just_audio playback + send-to-crew + inbox. **Migration 0007 you apply + create the bucket. Record/playback are device-only.**

---

## ⚠️ Tomorrow's punch-list (needs your device / Mac / accounts)

**Device tests (Android phone):**
- (to be filled as device features land)

**iOS Mac compile pass:**
- Existing: add `AlarmHostApiImpl.swift` to Runner target, bundle sound, wire entitlement (see `reliability/2026-07-20-ios-compile-checklist.md`).
- Plus: any new `Info.plist` usage strings + Swift I add tonight — compile-verify.

**Migrations to apply (Supabase):**
- (to be filled — I write them under `supabase/migrations/`, you run them)

**Accounts / store:**
- (RevenueCat, store products, etc. — to be filled)

---

## Decisions I made autonomously (flag if you disagree)
- (logged as they happen)
